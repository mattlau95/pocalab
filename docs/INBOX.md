# Inbox
- 1. The deck-section__header (label + preview) renders even on the empty home page
- 2. The deck-section__add "+ Add image" button appears before any cards are added, duplicating the main upload zone
- Both should only show once the deck has cards (or when there are multiple decks in multi-deck mode).